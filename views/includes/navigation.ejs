<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/"><img src="/favicon.ico" style="width:20px; height:20px"/></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mainNavigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNavigation">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= pagePath==='/' ? 'active': ''%>">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item <%= pagePath.startsWith('/admin') ? 'active': ''%>">
                <a class="nav-link" href="/admin">Admin</a>
            </li>
            <li class="nav-item <%= pagePath.startsWith('/shop') ? 'active': ''%>">
                <a class="nav-link" href="/shop">Shop</a>
            </li>
        </ul>
        <ul class="navbar-nav">

            <% if(isLoggedIn==undefined){ %>
            <li class="nav-item <%= pagePath.startsWith('/login') ? 'active': ''%>">
                <a class="nav-link" href="/login">Login</a>
            </li>

        <%} else {%>
            <li class="nav-item <%= pagePath.startsWith('/logout') ? 'active': ''%>">
                <a class="nav-link" href="/logout">Logout</a>
            </li>
        <% } %>
        </ul>
    </div>
</nav>
